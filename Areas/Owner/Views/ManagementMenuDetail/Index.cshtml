@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "MenuDetail";
}
@model IEnumerable<StoreManagement.Models.MenuDetail>
<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Description</th>
                <th>Picture</th>
                <th>FoodCategory</th>
                <th>UpdateTime</th>
                <th>Actions</th>
                <!-- Add other columns as needed -->
            </tr>
        </thead>
        <tbody>
            @foreach (var menuDetail in Model)
            {
                @* <tr>
                    <td>@menuDetail.FoodItem?.Name</td>
                    <td>@menuDetail.FoodItem?.Price</td>
                    <td>@menuDetail.FoodItem?.Description</td>
                    <td width="15%"><img width="100%" src="@menuDetail.FoodItem.ImageUrl" /></td>
                    <td>@(menuDetail.FoodItem?.FoodCategory?.Name)</td>
                    <td>@menuDetail.UpdateTime</td>
                    <td>
                        <a asp-action="Update" asp-route-Id="@menuDetail.Id">
                            <button type="submit" class="btn btn-info">Edit</button>
                        </a> |
                        <a asp-action="Delete" asp-route-id="@menuDetail.Id">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </a>|
                    </td>
                </tr> *@
            }
        </tbody>
    </table>

</div>

